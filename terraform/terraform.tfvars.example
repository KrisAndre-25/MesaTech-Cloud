# Copiar como terraform.tfvars (ignorado por git) y ajustar si es necesario.
# Los valores por defecto en variables.tf ya coinciden con el entorno
# local (docker-compose) y el tenant real de Microsoft Entra ID.

# db_password       = "adminpassword"
# allowed_ssh_cidr  = "TU_IP_PUBLICA/32"
# cors_allowed_origins = ["http://localhost:3000", "https://tu-frontend-desplegado.com"]
